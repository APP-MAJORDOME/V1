apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: majordome
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          image: majordome/backend:latest
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: majordome
spec:
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
