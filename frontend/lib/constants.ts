/** Durée d’affichage des toasts (pushToast). */
export const TOAST_DURATION_MS = 3200;

/** Fenêtre « alerte frigo » : expiration dans les N prochaines heures. */
export const FRIDGE_ALERT_HOURS_AHEAD = 48;

/** Terminées incluses au premier chargement (alléger la réponse API). */
export const INITIAL_DONE_TASKS_LIMIT = 100;

/** Rafraîchissement manuel « depuis le serveur ». */
export const DONE_HISTORY_FETCH_LIMIT = 200;
