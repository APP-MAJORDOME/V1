# Frontend MVP

Ce frontend est un cockpit web minimal Next.js.

## Démarrage local

```bash
npm install
npm run dev
```

Configurer `NEXT_PUBLIC_API_BASE` si l'API n'est pas sur `http://localhost:8000`.
